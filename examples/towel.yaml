# Project configuration can select only routes already authorized by the
# trusted --allow-route flags and can only reduce encrypted vault limits.
routes:
  - application
limits:
  max_requests: 5
  session_expiry_seconds: 300
